class UserClass {
  String name, location, serviceType, timings, url, bio;
  int salary, rating;

  UserClass(this.name, this.location, this.serviceType, this.salary,
      this.rating, this.timings, this.url, this.bio);
}
